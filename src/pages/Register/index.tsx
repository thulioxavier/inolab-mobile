import React, { Fragment } from "react";
import { Text } from "react-native";

export const Register = () => {
    return(
        <Fragment>
            <Text>Register</Text>
        </Fragment>
    )
}